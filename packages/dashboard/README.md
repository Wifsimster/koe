# @koe/dashboard

Admin panel for Koe. React + TanStack Router + Tailwind + shadcn/ui.

## Dev

```bash
pnpm dev
```

Opens on http://localhost:5173 by default. Auth is handled by `better-auth` via the API.
